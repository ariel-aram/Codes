[project]
name = "codes-bd"
version = "1.0.0"
description = "Redeemable codes system for BallsDex"
requires-python = ">=3.14"
# dependencies = [
#     "ballsdex>=3.0.0",
# ]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["codes_app"]
